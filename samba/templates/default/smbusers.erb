# This file maps windows usernames to local unix names
#
# format is map_to = map_from
#
# map_from may be a list of values, names with whitespace should be 
# surrounded by double quotes
#
# It was dynamically generated by Chef on <%= node["fqdn"] %>
#
# Local modifications will be overwritten by Chef.
#
<% node["smbusermap"]["maps"].each do |t,f| -%>
<%= t %> = <%= f.respond_to?(:join) ? f.join(',') : f %>
<% end -%>
